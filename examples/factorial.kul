# Returns factorial number n
def int factorial(int n):
	if (n <= 1):
		return 1;
	end
	return n * factorial(n - 1);
end
int number;
puts("Enter a number: ");
number = geti();
puts("The factorial number is: ");
puti(factorial(number));
puts("\n");

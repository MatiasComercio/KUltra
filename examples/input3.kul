def void voidFun(str arg1, int arg2):
    puts("arg1 is: ");
    puts(arg1);
    puts("\n");

    int var1;
    var1 = arg2;
    var1 = arg2 * 2 + 1 - 5/2 % 2;
    if (var1 < 3):
        puts("var1: "); puts(var1); puts(" is less than 3\n");
    else:
        puts("var1: "); puts(var1); puts(" is greater or equal than 3\n");
    end

    return;
end

voidFun("hello from voidFun. This is arg1", 5);